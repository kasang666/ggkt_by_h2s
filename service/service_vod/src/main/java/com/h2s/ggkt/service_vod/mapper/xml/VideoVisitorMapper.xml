<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.h2s.ggkt.service_vod.mapper.VideoVisitorMapper">

    <select id="findCount" resultType="com.h2s.ggkt.vo.vod.VideoVisitorCountVo">
    select
           date(join_time) as joinTime,
           count(*) as userCount
    from video_visitor
    <where>
        <if test="startTime != null and startTime != ''">
            and date(join_time) >= #{startTime}
        </if>
        <if test="endTime != null and endTime != ''">
            and date(join_time) &lt;= #{endTime}
        </if>
        and course_id=#{courseId}
    </where>
        group by date(join_time)
        order by date(join_time)
    </select>
</mapper>
